[package]
name = "platform-linux"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "XXKey platform adapter (platform-linux)"

[[bin]]
name = "xxkey-daemon"
path = "src/main.rs"

[dependencies]
vietime-engine = { path = "../vietime-engine" }
